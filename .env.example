# ── Twitter / X Bot (optional — only needed for bot/bot.ts) ─────────────────
# Get credentials at https://developer.twitter.com/en/portal/dashboard
# Requires a Project + App with Read+Write OAuth 1.0a permissions

TWITTER_API_KEY=
TWITTER_API_SECRET=
TWITTER_ACCESS_TOKEN=
TWITTER_ACCESS_SECRET=

# ── Probable Markets API (optional — only needed for bot scripts) ────────────
PROBABLE_API_BASE=https://api.probable.markets

# How many markets to post per bot run (default: 1)
MAX_POSTS=1

# ── Extension ────────────────────────────────────────────────────────────────
# The extension itself has NO server-side secrets.
# All signing happens inside MetaMask.
# No values required here for the Chrome extension to work.
